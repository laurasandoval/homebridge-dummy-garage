{
  "pluginAlias": "DummyGarage",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "title": "Dummy Garage",
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Dummy Garage",
        "description": "Name of Garage to appear in Home app"
      },
      "openingStateDuration": {
        "title": "\"Opening\" State Duration",
        "type": "integer",
        "default": 5,
        "description": "Number of seconds the door should spend \"opening\"."
      },
      "openStateDuration": {
        "title": "\"Open\" State Duration",
        "type": "integer",
        "default": 10,
        "description": "Number of seconds the door should stay open."
      },
      "closingStateDuration": {
        "title": "\"Closing\" State Duration",
        "type": "integer",
        "default": 5,
        "description": "Number of seconds the door should spend \"closing\"."
      }
    }
  }
}